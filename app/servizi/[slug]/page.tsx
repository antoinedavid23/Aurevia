import {notFound} from "next/navigation";import {PageHero,CTA} from "@/components/PageHero";import {services} from "@/data/content";
export default async function Page({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const service=services.find(x=>x.slug===slug);if(!service)notFound();return <><PageHero label="Services" title={service.title} text={service.short} image={service.image}/><section className="content-section ivory"><div className="container split"><div><p className="eyebrow dark">L’enjeu</p><h2>Plus de temps, plus de contrôle, moins d’imprévus</h2></div><div><p>Gérer une propriété exige continuité, partenaires fiables et attention. AUREVIA coordonne activités, communications et contrôles grâce à un interlocuteur unique.</p><ul className="feature-list"><li>Analyse initiale</li><li>Standards partagés</li><li>Coordination locale</li><li>Rapports clairs</li></ul></div></div></section><section className="content-section"><div className="container"><p className="eyebrow">Le processus</p><h2>Un service calibré pour votre propriété</h2><div className="steps">{["Échange et visite","Plan opérationnel","Mise en place","Suivi continu"].map((x,i)=><div key={x}><span>0{i+1}</span><h3>{x}</h3></div>)}</div></div></section><CTA/></>}
