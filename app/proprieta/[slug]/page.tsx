import { notFound } from "next/navigation";import { properties } from "@/data/content";import { PageHero,CTA } from "@/components/PageHero";
export function generateStaticParams(){return properties.map(p=>({slug:p.slug}))}
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=properties.find(x=>x.slug===slug);if(!p)notFound();return <><PageHero label={p.location} title={p.name} text="Una proprietà dimostrativa presentata per illustrare l’esperienza editoriale AUREVIA." image={p.image}/><section className="section ivory"><div className="container split"><div className={`property-image property-detail-image tone-${p.tone}`} style={{backgroundImage:`linear-gradient(180deg,transparent,rgba(7,16,25,.35)),url(${p.image})`}}/><div><p className="eyebrow dark">DÃ©tails</p><h2>Un refuge ligure soignÃ© dans chaque dÃ©tail.</h2><p>{p.bedrooms} chambres · {p.guests} voyageurs · {p.baths} salles de bain</p><ul className="feature-list"><li>Vue remarquable</li><li>Linge premium</li><li>Assistenza voyageurs</li><li>Conciergerie sur demande</li></ul><p className="demo-note">Proprietà e contenuti presentati a scopo dimostrativo.</p></div></div></section><CTA/></>}
