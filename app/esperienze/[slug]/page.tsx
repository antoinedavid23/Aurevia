import {notFound} from "next/navigation";import {PageHero,CTA} from "@/components/PageHero";import {experiences} from "@/data/content";
export default async function Page({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const e=experiences.find(x=>x.slug===slug);if(!e)notFound();return <><PageHero label="Expérience sur mesure" title={e.title} text={e.short} image={e.image}/><section className="section ivory"><div className="container split"><div><p className="eyebrow dark">L’expérience</p><h2>Conçue autour de votre rythme</h2></div><div><p>De la première demande à la confirmation, nous coordonnons partenaires et détails pour une expérience fluide et confidentielle.</p><ul className="feature-list"><li>Proposition personnalisée</li><li>Partenaires sélectionnés</li><li>Coordination dédiée</li><li>Confirmation transparente</li></ul><p className="demo-note">Sous réserve de disponibilité.</p></div></div></section><CTA/></>}
