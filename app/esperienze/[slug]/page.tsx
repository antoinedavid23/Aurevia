import {notFound} from "next/navigation";import {experiences} from "@/data/content";import {PageHero,CTA} from "@/components/PageHero";
export function generateStaticParams(){return experiences.map(e=>({slug:e.slug}))}
export default async function Page({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const e=experiences.find(x=>x.slug===slug);if(!e)notFound();return <><PageHero label="Esperienze su misura" title={e.title} text={e.short} image={e.image}/><section className="section ivory"><div className="container split"><div><p className="eyebrow dark">L’esperienza</p><h2>Costruita intorno ai tuoi tempi.</h2></div><div><p>Dalla prima richiesta alla conferma, coordiniamo partner e dettagli per offrire un momento fluido, riservato e coerente con il soggiorno.</p><ul className="feature-list"><li>Proposta personalizzata</li><li>Partner selezionati</li><li>Coordinamento dedicato</li><li>Conferma trasparente</li></ul><p className="demo-note">Soggetto a disponibilità.</p></div></div></section><CTA/></>}
