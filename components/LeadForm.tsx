"use client";import {useState} from "react";import {useRouter} from "next/navigation";
export function LeadForm({kind="contact"}:{kind?:"contact"|"valuation"}){const [status,setStatus]=useState("");const router=useRouter();async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setStatus("Invio in corso…");const data=Object.fromEntries(new FormData(e.currentTarget));const res=await fetch(`/api/${kind}`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(data)});if(res.ok){if(kind==="valuation"){router.push("/grazie")}else{setStatus("Messaggio ricevuto. Ti contatteremo in modo riservato.");}}else setStatus("Non è stato possibile inviare. Riprova tra poco.");}
 return <form onSubmit={submit}><input className="honeypot" name="website" tabIndex={-1} autoComplete="off"/><div className="field-row"><label>Nome<input required name="name" maxLength={80}/></label><label>Cognome<input required name="surname" maxLength={80}/></label></div><div className="field-row"><label>Email<input required name="email" type="email" maxLength={160}/></label><label>Telefono<input name="phone" type="tel" maxLength={40}/></label></div>{kind==="valuation"&&<><div className="field-row"><label>Città<input required name="city"/></label><label>Tipologia<select name="type"><option>Appartamento</option><option>Attico</option><option>Villa</option><option>Casa indipendente</option></select></label></div><div className="field-row"><label>Superficie m²<input name="area" type="number" min="20"/></label><label>Camere<input name="bedrooms" type="number" min="1"/></label></div><label>Obiettivo principale<select name="objective"><option>Delegare la gestione</option><option>Aumentare i ricavi</option><option>Migliorare l’esperienza degli ospiti</option><option>Proteggere e mantenere la proprietà</option><option>Ricevere una valutazione</option></select></label></>}<label>Messaggio<textarea required name="message" maxLength={2000}/></label><label><span><input required type="checkbox" name="consent"/> Accetto l’utilizzo dei dati per essere ricontattato sulla mia richiesta.</span></label><button className="button" type="submit">{kind==="valuation"?"Invia la richiesta riservata":"Invia il messaggio"}</button><div role="status" className="form-status">{status}</div></form>}
