"use client";import {useState} from "react";import {useRouter} from "next/navigation";
export function LeadForm({kind="contact"}:{kind?:"contact"|"valuation"}){const [status,setStatus]=useState("");const router=useRouter();async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setStatus("Invio in corso…");const data=Object.fromEntries(new FormData(e.currentTarget));const res=await fetch(`/api/${kind}`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(data)});if(res.ok){if(kind==="valuation"){router.push("/grazie")}else{setStatus("Message ricevuto. Ti contatteremo in modo riservato.");}}else setStatus("Nonn è stato possibile inviare. Riprova tra poco.");}
 return <form onSubmit={submit}><input className="honeypot" name="website" tabIndex={-1} autoComplete="off"/><div className="field-row"><label>PrÃ©nom<input required name="name" maxLength={80}/></label><label>Nonm<input required name="surname" maxLength={80}/></label></div><div className="field-row"><label>Email<input required name="email" type="email" maxLength={160}/></label><label>TÃ©lÃ©phone<input name="phone" type="tel" maxLength={40}/></label></div>{kind==="valuation"&&<><div className="field-row"><label>Città<input required name="city"/></label><label>Type de bien<select name="type"><option>Appartement</option><option>Penthouse</option><option>Villa</option><option>Maison indÃ©pendante</option></select></label></div><div className="field-row"><label>Surface m²<input name="area" type="number" min="20"/></label><label>Camere<input name="bedrooms" type="number" min="1"/></label></div><label>Objectif principal<select name="objective"><option>DÃ©lÃ©guer la gestion</option><option>Augmenter les revenus</option><option>Migliorare l’esperienza degli voyageurs</option><option>Proteggere e mantenere la proprietà</option><option>Recevoir une Ã©valuation</option></select></label></>}<label>Message<textarea required name="message" maxLength={2000}/></label><label><span><input required type="checkbox" name="consent"/> Accetto l’utilizzo dei dati per essere ricontattato sulla mia richiesta.</span></label><button className="button" type="submit">{kind==="valuation"?"Envoyer la demande confidentielle":"Envoyer le message"}</button><div role="status" className="form-status">{status}</div></form>}
