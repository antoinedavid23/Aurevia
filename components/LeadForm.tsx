"use client";
import {useState} from "react";import {useRouter} from "next/navigation";
export function LeadForm({kind="contact"}:{kind?:"contact"|"valuation"}){
 const [status,setStatus]=useState("");const router=useRouter();
 async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setStatus("Envoi en cours…");const data=Object.fromEntries(new FormData(e.currentTarget));const res=await fetch(`/api/${kind}`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(data)});if(res.ok){if(kind==="valuation")router.push("/grazie");else setStatus("Message reçu. Nous vous répondrons de manière confidentielle.");}else setStatus("L’envoi a échoué. Veuillez réessayer dans quelques instants.");}
 return <form onSubmit={submit}><input className="honeypot" name="website" tabIndex={-1} autoComplete="off"/><div className="field-row"><label>Prénom<input required name="name" maxLength={80}/></label><label>Nom<input required name="surname" maxLength={80}/></label></div><div className="field-row"><label>E-mail<input required name="email" type="email" maxLength={160}/></label><label>Téléphone<input name="phone" type="tel" maxLength={40}/></label></div>{kind==="valuation"&&<><div className="field-row"><label>Ville<input required name="city"/></label><label>Type de bien<select name="type"><option>Appartement</option><option>Attique</option><option>Villa</option><option>Maison indépendante</option></select></label></div><div className="field-row"><label>Surface en m²<input name="area" type="number" min="20"/></label><label>Chambres<input name="bedrooms" type="number" min="1"/></label></div><label>Objectif principal<select name="objective"><option>Déléguer la gestion</option><option>Optimiser les revenus</option><option>Améliorer l’expérience des voyageurs</option><option>Protéger et entretenir le bien</option><option>Recevoir une évaluation</option></select></label></>}<label>Message<textarea required name="message" maxLength={2000}/></label><label><span><input required type="checkbox" name="consent"/> J’accepte que mes données soient utilisées afin d’être recontacté au sujet de ma demande.</span></label><button className="button" type="submit">{kind==="valuation"?"Envoyer la demande confidentielle":"Envoyer le message"}</button><div role="status" className="form-status">{status}</div></form>;
}
